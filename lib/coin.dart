import 'package:flutter/material.dart';

class MyCoin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      width: 40,
      child: Image.asset('images/dogecoin.png'),
    );
  }
}
